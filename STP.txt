問１
Switch3

問２
Switch1　F0/1 指定ポート(DP)
         F0/2 ルートポート(RP)

Switch2    F0/1 ルートポート(RP)
         F0/2 ブロッキングポート(BP)

Switch3  E0/1 指定ポート(DP)
         E0/2 指定ポート(DP)

Switch4  E0/1 指定ポート(DP)
         E0/2 ルートポートポート(RP)


